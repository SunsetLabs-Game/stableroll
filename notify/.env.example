# notify/ has no environment variables. Content topics and encryption keys
# are derived deterministically from the commitment secret / recipient public
# key (see src/topics.ts), and the Waku Light Push connection needs no
# credentials. This file exists so the absence is explicit rather than
# undocumented.
